// Nginx is a powerful web server and uses a non-threaded,
// event-driven architecture.

// it can also do other important things, such as load blancing,
// and HTTP caching, or be used as a reverse proxy.

// advantage of nginx

// can handle 10000 concurrent requests
// cache http requests
// acts as reverse proxy
// act as load blancer
// act as an Api gateway
// serve and cache static files like imgaes, videos, etc.
// handle SSL Certificates..